<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> pt-alert-container">
    <a href="#" data-dismiss="alert" class="close">×</a>
      <ul class="pt-alert">
        <li>
          <%= value %>
        </li>
      </ul>
  </div>
<% end %>